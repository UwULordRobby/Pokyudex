version: '3.8'

services:
  pokyudex-web:
    build: .
    container_name: pokyudex-web
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped